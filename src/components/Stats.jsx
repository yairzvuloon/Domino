import React from "react";
import Home from "screen"

const Stats = props => {
    const {turn,number,pieace} = props;
    let timer =0;
    // todo: create timer that will start at begining of game and at the end of turn will update
    //

};
export default Stats;
